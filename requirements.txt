# HuggingFace Spaces installs from this file on build (#51, Phase 7 / slice 1).
# Mirrors the top-level deps in pyproject.toml; Spaces resolves transitives.
# Keep in sync with pyproject.toml when bumping versions — there is no
# automated reconciliation between the two.
anthropic>=0.49.0
chromadb>=0.6.0
gradio>=5.22.0
huggingface-hub>=0.27.0
litellm>=1.83.0
matplotlib>=3.10.9
numpy>=2.3.3
openai>=1.68.2
pydantic>=2.0
python-dotenv>=1.0.1
tenacity>=9.1.2
tqdm>=4.67.0
